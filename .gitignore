# Environment variables (secrets)
.env
.env.*

# OS
.DS_Store

# Ruby LSP (editor tooling)
.ruby-lsp/

# Claude Code (local settings only)
.claude/settings.local.json

# Local data backups (may contain personal notes)
exercise_notes_backup_*.csv
